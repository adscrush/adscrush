"use client"

/**
 * Audit log component is disabled because the media_audit_logs table has been removed.
 * The admin page no longer includes this component.
 */
export function AuditLog() {
  return null
}
