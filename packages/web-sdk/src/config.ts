/**
 * Global SDK constants
 */

/**
 * Default tracking domain (hardcoded for production)
 */
export const DEFAULT_TRACKING_DOMAIN = "track.adscrush.com"

/**
 * Default cookie expiration in days
 */
export const DEFAULT_COOKIE_EXPIRY = 30

/**
 * Default URL parameter name for click ID
 */
export const DEFAULT_PARAM_NAME = "tid"

/**
 * Storage key for click ID
 */
export const CLICK_ID_KEY = "adscrush_tid"

/**
 * Storage key prefix for deduplication flags
 */
export const DEDUP_KEY_PREFIX = "adscrush_dedup_"
